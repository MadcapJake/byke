require: knex

provide: create-table

print knex

macro{knex} create-table(`{^name, ^body}`) =
   let c-t = @deps.knex.schema.create-table
   let it = @env.mark(`@`)
   ```
   [^c-t](^name) with table ->
      let ^it = table
      ^body
   ```
